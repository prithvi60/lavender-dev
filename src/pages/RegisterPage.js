import Register from '../features/Login/Register';

const RegisterPage = () => {
  return (
      <div className="login-page">
          <Register />
      </div>
  );
};

export default RegisterPage;
